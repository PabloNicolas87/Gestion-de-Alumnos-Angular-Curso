import { TituloGrandeDirective } from './titulo-grande.directive';

describe('TituloGrandeDirective', () => {
  it('should create an instance', () => {
    const directive = new TituloGrandeDirective();
    expect(directive).toBeTruthy();
  });
});
